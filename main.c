#include <stdio.h>

void bubbleSort(int numbers[], int size);

int main(int argc, const char* argv[]) {

    int numbers[] = { 12, 15, 6, 100, 23, 78, 1, 0, 16 };
    int size = sizeof(numbers) / sizeof(int);
    printf("%d\n", numbers[0]);
    printf("Size of array is %d.\n", size);

    return 0;
}

void bubbleSort(int numbers[], int size) {
    
}